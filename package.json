{
  "name": "q-then",
  "version": "0.7.2",
  "main": "index.js",
  "description": "very fast nodejs promises engine",
  "license": "Apache-2.0",
  "repository": {
    "url": "git://github.com/andrasq/node-q-then",
    "type": "git"
  },
  "engines": {
    "node": "^0.10"
  },
  "keywords": [ "fast", "promise", "Andras", "async" ],
  "scripts": {
    "coverage": "nyc --version && nyc --reporter lcov --reporter text qnit test/test-q* || echo nyc not installed",
    "clean": "rm -rf .nyc_output coverage npm-debug.log",
    "test": "qnit --version && qnit test/test-q* && node -p 'require(\"promises-aplus-tests\")' && node test/test-aplus* || echo qnit not installed, cannot test"
  }
}
