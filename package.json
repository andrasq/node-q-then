{
  "name": "q-promise",
  "version": "0.4.1",
  "main": "index.js",
  "description": "very fast nodejs promises engine",
  "license": "Apache-2.0",
  "repository": {
    "url": "git://github.com/andrasq/node-q-promise",
    "type": "git"
  },
  "engines": {
    "node": "^0.10"
  },
  "keywords": [ "fast", "promise", "Andras", "async" ],
  "scripts": {
    "coverage": "nyc --version && nyc --reporter lcov --reporter text qnit test/test* || echo nyc not installed",
    "clean": "rm -rf .nyc_output coverage npm-debug.log",
    "test": "qnit --version && qnit test/test* || echo qnit not installed, cannot test"
  }
}
